function int* updateArray(int a, int[]* tab, int size) {
    for (int i = 0; i < len(tab); i++) {
        tab[i] = tab[i] * 2;
    };
    int a = tab[0];
    return &a;
};

int[] tab = [1, 2, 3];

int a = 5;
int b = 6;
int* pa = &a;
print(*pa);
*pa = 7;


// print before update
print(tab);

updateArray(6);

// print after update
print(tab);