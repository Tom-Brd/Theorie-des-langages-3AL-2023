function int[] getTab(){
    int tab[] = [];
    return tab;
};

int tab[] = getTab();
tab[0] = 20;
print(tab[0]);
print(len(tab))

// Créer la fonction len qui renvoie la longueur d'un tableau, d'une chaine de caractères ou le nombre de digit d'un int
// Créer le lexeur pour les syntaxes évoquées plus haut
// Créer les types de retour int[]
// Créer les variables tab[]